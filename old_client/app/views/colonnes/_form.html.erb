<%= form_with(model: colonne, local: true) do |form| %>
  <% if colonne.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(colonne.errors.count, "error") %> prohibited this colonne from being saved:</h2>

      <ul>
        <% colonne.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :shelf_id %>
    <%= form.text_field :shelf_id %>
  </div>

  <div class="field">
    <%= form.label :width %>
    <%= form.text_field :width %>
  </div>

  <div class="field">
    <%= form.label :center_x %>
    <%= form.text_field :center_x %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
